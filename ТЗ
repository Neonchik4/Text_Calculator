1. Формат ввода:
    - целые числа от -99 до 99 (включительно), записанные словами
    - числа и операции разделяются пробелами
    - слово "минус" означает, что следующее число будет отрицательным
    - между словами и операциями должны стоять пробелы
    - дробные числа использовать нельзя
2. Формат вывода: целое число
3. Реализация глобальных словарей для чисел и цифр, а также для операций сложения, умножения, вычитания и деления
4. Сделать функцию text_to_number, которая превращает составное число (прим. "тридцать три") в число
5. Написать функцию calculate, которая считает значение выражения и возвращает его
Примечания:
    - все операции выполняются последовательно вне зависимости от приоритета операции
    - операция минус (в знач. отрицательного числа) допустима только в начале выражения, т.е. нельзя написать следующее
"два умножить на минус два"
